version: 1.0
runtime: nodejs18
build:
  commands:
    build:
      - yarn install --frozen-lockfile
      - yarn build
run:
  runtime-version: 18.19.0
  command: yarn start
  network:
    port: 3000
    env: PORT
  env:
    PORT: 3000