version: 1.0
runtime: nodejs18
install:
  command: yarn install --frozen-lockfile
build:
  command: yarn build
run:
  runtime-version: 18.19.0
  command: yarn start
  network:
    port: 3000
    env: PORT