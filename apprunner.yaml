version: 1.0
runtime: nodejs22
build:
  commands:
    pre-build:
      - yarn install --frozen-lockfile
    build:
      - yarn build
run:
  runtime-version: 22.14.0
  command: yarn start
  network:
    port: 3000
    env: PORT